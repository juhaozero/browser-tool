# 部署子路径，必须以 / 开头并以 / 结尾；根路径部署保持 /
# 注意：Tauri 桌面打包时会自动忽略此项，强制使用相对路径 ./
VITE_BASE_PATH=/

# 子路径部署示例（访问 https://example.com/app/）
# VITE_BASE_PATH=/app/

# 站点绝对源（无尾斜杠），用于生成 sitemap / robots.txt
# VITE_SITE_ORIGIN=https://example.com
